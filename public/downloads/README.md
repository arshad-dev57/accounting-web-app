# Desktop installers are not shipped in this repo (breaks Vercel / Git LFS).
# Local: set DESKTOP_APP_MAC_PATH / DESKTOP_APP_WIN_PATH
# Production: set NEXT_PUBLIC_DESKTOP_DOWNLOAD_MAC / WIN to a CDN URL
